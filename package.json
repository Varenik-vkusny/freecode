{
  "name": "freecode-root",
  "version": "1.0.0",
  "description": "FreeCode Root Workspace",
  "scripts": {
    "tauri": "tauri",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "@tauri-apps/cli": "latest"
  }
}
